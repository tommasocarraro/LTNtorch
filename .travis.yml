language: python
python:
  - "3.9"      # current default Python on Travis CI

# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install pytest-cov
  - pip install coveralls

# command to run tests
script:
  - python -m pytest --cov=LTNtorch

after_success:
  - coveralls